<div class="width-limiter">

<div id="main-content" class="section content">

<div class="main-heading">

<div class="rustdoc-breadcrumbs">

[vstd](../../index.html)::[arithmetic](../index.html)

</div>

# Module overflow Copy item path

<span class="sub-heading"><a href="../../../src/vstd/arithmetic/overflow.rs.html#1-307"
class="src">Source</a> </span>

</div>

## Structs<a href="#structs" class="anchor">§</a>

<a href="struct.CheckedU8.html" class="struct"
title="struct vstd::arithmetic::overflow::CheckedU8">CheckedU8</a>  
This struct represents a `$uty` value that can overflow. The `i` field
is a ghost value that represents the true value, while the `v` field is
`None` when the value has overflowed and `Some(x)` when the value `x`
fits in a `$uty`.

<a href="struct.CheckedU16.html" class="struct"
title="struct vstd::arithmetic::overflow::CheckedU16">CheckedU16</a>  
This struct represents a `$uty` value that can overflow. The `i` field
is a ghost value that represents the true value, while the `v` field is
`None` when the value has overflowed and `Some(x)` when the value `x`
fits in a `$uty`.

<a href="struct.CheckedU32.html" class="struct"
title="struct vstd::arithmetic::overflow::CheckedU32">CheckedU32</a>  
This struct represents a `$uty` value that can overflow. The `i` field
is a ghost value that represents the true value, while the `v` field is
`None` when the value has overflowed and `Some(x)` when the value `x`
fits in a `$uty`.

<a href="struct.CheckedU64.html" class="struct"
title="struct vstd::arithmetic::overflow::CheckedU64">CheckedU64</a>  
This struct represents a `$uty` value that can overflow. The `i` field
is a ghost value that represents the true value, while the `v` field is
`None` when the value has overflowed and `Some(x)` when the value `x`
fits in a `$uty`.

<a href="struct.CheckedU128.html" class="struct"
title="struct vstd::arithmetic::overflow::CheckedU128">CheckedU128</a>  
This struct represents a `$uty` value that can overflow. The `i` field
is a ghost value that represents the true value, while the `v` field is
`None` when the value has overflowed and `Some(x)` when the value `x`
fits in a `$uty`.

<a href="struct.CheckedUsize.html" class="struct"
title="struct vstd::arithmetic::overflow::CheckedUsize">CheckedUsize</a>  
This struct represents a `$uty` value that can overflow. The `i` field
is a ghost value that represents the true value, while the `v` field is
`None` when the value has overflowed and `Some(x)` when the value `x`
fits in a `$uty`.

</div>

</div>
